Config = {
	Lan = "en",

	System = {
		Debug = false,
		Notify = "gta", -- gta, ox, qb, esx, okok, red
		drawText = "gta", -- gta, ox, qb, esx, red
	},

	General = {
		ShowTrainBlips = true,			-- Show nearby train blips
		showStationBlips = true, 		-- Show train station blips
		requireMetroTicket = true,		-- Do metro trains require tickets
		chargeBank = true,				-- charge players bank account
		chargeAmount = 100,				-- amount per ticket
		societyAccount = "cityhall",	-- Society account to fund when

		seatPlayer = true,				-- Forcibly seat player in train seat, if false they are allowed to walk around
	},
}